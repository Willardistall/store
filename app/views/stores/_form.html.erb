<h1><%= @store.id ? 'Edit Store' : 'Create Store' %></h1>
<hr />
<%= form_for [@store] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <br />
  <%= f.label :body %>
  <%= f.text_area :body, required: true %>
  <%= f.submit %>
<% end %>